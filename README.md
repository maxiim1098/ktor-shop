# 🛒 Ktor Shop Backend

Backend-сервис интернет-магазина на **Kotlin** с использованием **Ktor**, **PostgreSQL**, **Redis** и **RabbitMQ**.  
Проект выполнен в рамках учебного задания и полностью соответствует требованиям (кроме платного деплоя).

---

## 📋 Содержание
1. [Архитектура проекта](#архитектура-проекта)
2. [Структура модулей](#структура-модулей)
3. [Технологический стек](#технологический-стек)
4. [Запуск приложения](#запуск-приложения)
    - [Через Docker (рекомендовано)](#через-docker-рекомендовано)
    - [Локально без Docker](#локально-без-docker)
5. [Тестирование](#тестирование)
    - [Запуск всех тестов](#запуск-всех-тестов)
    - [Ручное тестирование через Swagger](#ручное-тестирование-через-swagger)
    - [Ручное тестирование через curl](#ручное-тестирование-через-curl)
6. [Документация API (Swagger)](#документация-api-swagger)
7. [Описание эндпоинтов](#описание-эндпоинтов)
8. [Возможные проблемы и их решения](#возможные-проблемы-и-их-решения)
9. [старт](#копирование-проекта-для-тестирования)

---

## 🏗️ Архитектура проекта

Проект построен по принципам **чистой архитектуры** и разделён на несколько модулей Gradle. Это обеспечивает:

- **Разделение ответственности** – каждый модуль отвечает за свою зону.
- **Тестируемость** – можно тестировать слои изолированно.
- **Гибкость** – легко заменять реализации (например, кэш или БД).

**Слои:**
- **domain** – сущности (`data class`), интерфейсы репозиториев, DTO, утилиты (JWT, хеширование). Не зависит ни от чего.
- **data** – реализация репозиториев, работа с БД (Exposed, Flyway). Зависит от domain.
- **core** – бизнес-логика (сервисы), кэширование (Redis), продюсеры и консьюмеры RabbitMQ. Зависит от domain и data.
- **api** – HTTP-слой (контроллеры/роуты), плагины Ktor, Swagger, внедрение зависимостей (Koin). Зависит от core.

---

## 📁 Структура модулей (основные директории и файлы)



``` 
ktor-shop/
├── api/
│   ├── src/main/kotlin/com/example/api/
│   │   ├── Application.kt
│   │   ├── plugins/
│   │   │   ├── Auth.kt
│   │   │   ├── Routing.kt
│   │   │   ├── Serialization.kt
│   │   │   ├── Swagger.kt
│   │   │   └── DI.kt
│   └── src/test/kotlin/com/example/
├── core/
│   ├── src/main/kotlin/com/example/
│   │   ├── domain/service/
│   │   ├── infrastructure/
│   │   │   ├── redis/
│   │   │   ├── rabbitmq/
│   │   │   └── worker/
│   │   └── di/
│   └── src/main/resources/
│       └── application.conf
├── data/
│   ├── src/main/kotlin/com/example/data/
│   │   ├── tables/
│   │   ├── repository/
│   │   ├── migration/
│   │   └── config/
│   └── src/main/resources/db/migration/
├── domain/
│   ├── src/main/kotlin/com/example/domain/
│   │   ├── model/
│   │   ├── dto/
│   │   ├── repository/
│   │   ├── service/
│   │   └── util/
├── gradle/
├── docker-compose.yml
├── Dockerfile
├── gradlew, gradlew.bat
├── settings.gradle.kts
├── run-tests.ps1
└── README.md
```


> **Примечание:** Роуты (эндпоинты) определены в плагине `Routing.kt` или в отдельных файлах внутри `api/src/main/kotlin/com/example/api/routes/`. Swagger автоматически подхватывает все маршруты.

---

## 🛠️ Технологический стек

| Компонент         | Технология                            | Назначение                              |
|-------------------|---------------------------------------|-----------------------------------------|
| Язык              | Kotlin 1.9                            | Основной язык разработки                |
| Фреймворк         | Ktor 2.3.5                            | Создание HTTP-сервера                   |
| База данных       | PostgreSQL 15                          | Хранение данных                         |
| ORM               | Exposed 0.44.0                        | Работа с БД в Kotlin-стиле               |
| Миграции          | Flyway 9.22.3                         | Управление схемой БД                     |
| Кэширование       | Redis 7 (через Redisson 3.23.4)       | Кэш для товаров и заказов                |
| Очереди           | RabbitMQ 3-management (amqp-client 5.20.0) | Асинхронная обработка событий заказов   |
| Аутентификация    | JWT (Auth0, Ktor auth)                 | Защита эндпоинтов                        |
| Внедрение зависимостей | Koin 3.5.0                         | DI                                       |
| Документация API  | Swagger (ktor-swagger, ktor-openapi)  | Интерактивная документация               |
| Тестирование      | JUnit 5, Testcontainers, MockK        | Unit, интеграционные, e2e тесты          |
| Контейнеризация   | Docker, docker-compose                 | Локальный запуск и деплой                |

---

## 🚀 Запуск приложения

### Через Docker (рекомендовано)

1. Убедитесь, что установлены **Docker** и **Docker Compose**.
2. В корне проекта выполните:
   ```bash
   docker-compose up -d
   ```
3. Дождитесь, пока все контейнеры поднимутся (это займёт около 30 секунд). Проверить статус:
   ```bash
   docker-compose ps
   ```
4. Приложение будет доступно по адресу: http://localhost:8080
5. Остановить все контейнеры:
   ```bash
   docker-compose down
   ```
**Что произойдёт:**

- **Поднимется PostgreSQL на порту 5432, автоматически создастся база shop.**

- **Поднимется Redis на порту 6379.**

- **Поднимется RabbitMQ с веб-интерфейсом на порту 15672 (логин: guest/guest).**

- **Соберётся и запустится приложение на порту 8080. Оно применит миграции Flyway и подключится ко всем сервисам.**

---

## 🧪 Тестирование
### Запуск всех тестов
**В корне проекта выполните:**

```bash
./gradlew clean test
```
**После завершения отчёт о тестах будет доступен по пути:**

```text
api/build/reports/tests/test/index.html
```
**Откройте этот файл в браузере, чтобы увидеть детали.**

**Ожидаемый результат:**

- **8 тестов, из них:**

- **6 успешных (unit и integration)**

- **2 игнорированных (e2e, отключены по причине, описанной ниже)**

### Ручное тестирование через Swagger (рекомендовано)
1. Запустите приложение (через Docker или локально).

2. Откройте в браузере: http://localhost:8080/docs/index.html

3. Вы увидите список всех эндпоинтов. Нажмите на любой, затем кнопку Try it out, заполните параметры и отправьте запрос.

4. Для эндпоинтов, требующих авторизацию, сначала получите токен через /auth/login, затем нажмите кнопку Authorize вверху страницы и введите токен.

### Ручное тестирование через curl
**Примеры команд (можно выполнить в PowerShell или bash):**

**Регистрация нового пользователя**

```bash
curl -X POST http://localhost:8080/auth/register \
-H "Content-Type: application/json" \
-d '{"email":"test@example.com","password":"123456"}'
```
**Ожидаемый ответ: либо 201 Created с токеном, либо 400 Bad Request если пользователь уже существует.**

**Вход (получение токена)**

```bash
curl -X POST http://localhost:8080/auth/login \
-H "Content-Type: application/json" \
-d '{"email":"test@example.com","password":"123456"}'
```
**Ожидаемый ответ: JSON с полем "token".**

**Получение списка товаров (требуется токен)**

```bash
curl -X GET http://localhost:8080/products \
-H "Authorization: Bearer ВАШ_ТОКЕН"
```

---
## 📚 Документация API (Swagger)
### Swagger UI доступен по адресу:
http://localhost:8080/docs/index.html

**Он содержит:**

- **Описание всех эндпоинтов с методами, параметрами, телами запросов и ответов.**

- **Схемы моделей (User, Product, Order и т.д.).**

- **Возможность отправлять запросы прямо из браузера.**

**Важно: Swagger генерируется автоматически на основе кода роутов. Если какой-то эндпоинт не отображается, проверьте, что в Application.kt вызван configureSwagger() до configureRouting().**

---

🔌## 🔌 Описание эндпоинтов

### Публичные (не требуют токена)

| Метод | Путь             | Описание                          |
|-------|------------------|-----------------------------------|
| POST  | `/auth/register` | Регистрация нового пользователя  |
| POST  | `/auth/login`    | Вход, получение JWT токена       |
| GET   | `/health`        | Проверка работоспособности       |

### Требуют JWT (любой аутентифицированный пользователь)

| Метод | Путь                | Описание                                  |
|-------|---------------------|-------------------------------------------|
| GET   | `/products`         | Список всех товаров                       |
| GET   | `/products/{id}`    | Детальная информация о товаре             |
| POST  | `/orders`           | Создание нового заказа                    |
| GET   | `/orders`           | История заказов текущего пользователя    |
| DELETE| `/orders/{id}`      | Отмена заказа (если он ещё не обработан) |

### Требуют JWT + роль ADMIN

| Метод | Путь                | Описание                          |
|-------|---------------------|-----------------------------------|
| POST  | `/products`         | Добавить новый товар             |
| PUT   | `/products/{id}`    | Обновить данные товара           |
| DELETE| `/products/{id}`    | Удалить товар                    |
| GET   | `/stats/orders`     | Статистика по всем заказам       |

---

## ⚠️ Возможные проблемы и их решения

### 1. Приложение в Docker не отвечает (404 на любой запрос)

**Причина:** Возможно, приложение не запустилось из-за проблем с подключением к БД или Redis.

**Решение:**
- Проверьте логи контейнера:
  ```bash
  docker logs ktor-shop-app-1
  ```

1. **Приложение в Docker не отвечает (404 на любой запрос)**  
   **Причина:** Возможно, приложение не запустилось из-за проблем с подключением к БД или Redis.  
   **Решение:**
    - Проверьте логи контейнера:
      ```bash
      docker logs ktor-shop-app-1
      ```  
    - Убедитесь, что все контейнеры здоровы:
      ```bash
      docker-compose ps
      ```  
    - Если в логах ошибка `FATAL: database "shop" does not exist`, войдите в контейнер БД и создайте базу вручную:
      ```bash
      docker exec -it ktor-shop-db-1 psql -U postgres -c "CREATE DATABASE shop;"
      ```  
    - Перезапустите контейнер приложения:
      ```bash
      docker-compose restart app
      ```

2. **Swagger недоступен или отображает не все эндпоинты**  
   **Причина:** Неправильная настройка OpenAPI или отсутствие operationId.  
   **Решение:**
    - В логах приложения могут быть предупреждения об отсутствии operationId – это нормально, Swagger сам генерирует их.
    - Убедитесь, что в `Application.kt` вызван `configureSwagger()` до `configureRouting()`.
    - Попробуйте пересобрать образ:
      ```bash
      docker-compose build --no-cache app
      docker-compose up -d
      ```

3. **Тесты падают с ошибкой подключения к БД (Testcontainers)**  
   **Причина:** Testcontainers требует запущенный Docker и может конфликтовать с уже занятыми портами.  
   **Решение:**
    - Убедитесь, что Docker запущен.
    - Остановите локальные экземпляры PostgreSQL, если они занимают порт 5432.
    - Если тесты всё равно падают, попробуйте запустить их с правами администратора (Windows) или добавьте пользователя в группу docker (Linux).

4. **Ошибка `Serializer for class 'LocalDateTime' is not found`**  
   **Причина:** Не подключена зависимость kotlinx-datetime или не настроена сериализация.  
   **Решение:**
    - В файле `api/build.gradle.kts` уже есть зависимость `kotlinx-datetime`. Убедитесь, что она используется.
    - В моделях, где используется `java.time.LocalDateTime`, либо замените на `kotlinx.datetime.LocalDateTime` и добавьте `@Serializable`, либо настройте сериализацию через Ktor (в плагине Serialization.kt).

5. **После запуска через Docker не работает RabbitMQ (воркер не подключается)**  
   **Причина:** RabbitMQ может запускаться дольше, чем приложение, и воркер не успевает подключиться.  
   **Решение:** В логах видно, что воркер несколько раз переподключается и в итоге соединяется успешно. Если соединение не устанавливается, проверьте, что RabbitMQ доступен по имени `rabbitmq` (внутри сети Docker). В `application.conf` должен быть указан `rabbitmq.host = rabbitmq`. Если вы меняли имя сервиса в compose, исправьте.

6. **При запросе к `/health` возвращается 404**  
   **Причина:** В текущей реализации эндпоинт `/health` может отсутствовать или быть определён под другим путём. Проверьте наличие в логах строки `healthGet` – это означает, что Swagger его обнаружил. Если его нет, можно создать простой обработчик в `Routing.kt`:
   ```kotlin
   get("/health") { call.respondText("OK") }

---

   ## 📥 Быстрый старт с GitHub

Чтобы скачать проект и начать работать с ним, выполните следующие шаги:

1. **Клонируйте репозиторий**  
   Откройте терминал (PowerShell, bash или cmd) и выполните:
   ```bash
   git clone https://github.com/maxiim1098/ktor-shop.git
    ```

2. **Перейдите в папку проекта**
```bash
cd ktor-shop
```

3. **Запустите приложение через Docker (рекомендуется)**

```bash
docker-compose up -d
```
**После запуска API будет доступно по адресу http://localhost:8080/docs/index.html (Swagger UI).**
